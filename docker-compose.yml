services:
  dev:
    build:
      context: .
      target: development
    ports:
      - '4321:4321'
    volumes:
      - .:/app
      - /app/node_modules
      - persistent-data:/app/.persistent
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    networks:
      - datum-network
    command: npm run dev --

  prod:
    build:
      context: .
      target: production
    ports:
      - '4321:4321'
    volumes:
      - persistent-data:/app/.persistent
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4321
    env_file:
      - .env
    networks:
      - datum-network
    restart: unless-stopped

networks:
  datum-network:
    driver: bridge

volumes:
  persistent-data:
    driver: local
