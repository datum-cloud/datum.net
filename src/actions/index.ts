import { vote, unvote, getUserVoted, loginWithDatum } from './roadmap';
import { NewsletterSignup } from './newsletter';

export const server = {
  vote,
  unvote,
  getUserVoted,
  loginWithDatum,
  NewsletterSignup,
};
