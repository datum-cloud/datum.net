import { vote, unvote, getUserVoted, loginWithDatum } from './roadmap';
import { signup } from './newsletter';

export const server = {
  vote,
  unvote,
  getUserVoted,
  loginWithDatum,
  signup,
};
