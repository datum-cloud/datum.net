import { vote, unvote, getUserVoted, loginWithDatum } from './roadmap';
import { getStargazerCount } from './github';
export const server = {
  vote,
  unvote,
  getUserVoted,
  loginWithDatum,
  getStargazerCount,
};
