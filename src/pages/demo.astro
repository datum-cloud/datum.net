---
// src/pages/demo.astro
import '@styles/global.css';
import Layout from '@layouts/Layout.astro';
import Hero from '@components/Hero.astro';
import Footer from '@components/Footer.astro';
import Icon from '@components/Icon.astro';
import BookDemo from '@components/forms/BookDemo.astro';

const title = 'Book a demo';
const description = 'See our modern alternative to global cloud solutions in action.';

const checklist = [
  {
    title: 'Open, neutral network cloud',
  },
  {
    title: 'Built for AI',
  },
  {
    title: 'Focused on Providers',
  },
];
---

<Layout title={title} description={description} bodyClass="page-demo">
  <section class="datum-container">
    <Hero class="light" hideContent={true} />

    <div class="section--block relative overflow-hidden bg-white px-7">
      <div class="max-width-nav large-pad relative">
        <span class="section-line section-line--left section-line--top"></span>
        <span class="section-eyebrow section-eyebrow--left-top-keyline">Book a demo</span>

        <div class="max-width-nopad">
          <div class="grid grid-cols-6 gap-8 md:gap-15 lg:gap-25 xl:gap-28">
            <div class="col-span-6 flex flex-col gap-10 md:col-span-3">
              <div class="flex flex-col gap-5">
                <h1 class="datum-text-8xl text-midnight-fjord">{title}</h1>
                <p class="datum-text-lg text-app---dark---utility-3">{description}</p>
              </div>

              <ul class="flex flex-col gap-7">
                {
                  checklist.map((item) => (
                    <li class="flex items-start gap-4">
                      <div class="bg-aurora-moss flex size-10 shrink-0 items-center justify-center">
                        <Icon name="check" size="md" class="text-app---dark---utility-3 stroke-1" />
                      </div>
                      <div class="flex flex-col gap-1">
                        <p class="datum-text-lg text-midnight-fjord font-semibold">{item.title}</p>
                      </div>
                    </li>
                  ))
                }
              </ul>
            </div>

            <div class="col-span-6 md:col-span-3">
              <BookDemo />
            </div>
          </div>
        </div>
      </div>
    </div>

    <Footer showCTA={false} plainIllustration />
  </section>
</Layout>
